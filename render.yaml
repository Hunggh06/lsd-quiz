services:
  - type: web
    name: lsd-quiz
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /
    previews:
      generation: automatic
